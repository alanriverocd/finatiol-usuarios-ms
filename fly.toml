app = "REPLACE_WITH_APP_NAME"

[build]
  image = ""
  dockerfile = "Dockerfile"

[env]
  # Set runtime environment variables via Fly secrets instead of committing them here
  # Example:
  # SPRING_DATASOURCE_URL = "jdbc:postgresql://..."
